# Apply Integration Fixtures

This directory contains fixture notes for selected-platform apply integration tests.

- Tests create temporary project directories at runtime.
- No static fixture payload is required for the current apply flow.
